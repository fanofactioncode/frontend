// import { cookies } from "next/headers";

// type Preferences = {
//   city: string | null;
// };

// export async function setPreferences({ city }: Preferences) {
//   const cookieStore = await cookies();
// }

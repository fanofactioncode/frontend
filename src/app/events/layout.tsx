import React from "react";

import PageLayout from "@/components/layout/page-layout";

export default function EventsLayout({ children }: React.PropsWithChildren) {
  return <PageLayout>{children}</PageLayout>;
}
